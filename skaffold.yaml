apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/liatrio/logstash-input-jira:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/liatrio/logstash-input-jira:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: logstash-input-jira
        chartPath: charts/logstash-input-jira
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/liatrio/logstash-input-jira'
          image.tag: '{{.DIGEST_HEX}}'
